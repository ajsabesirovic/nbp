services:
  mongo:
    image: mongo:7
    container_name: fitjourney-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - fitjourney-mongo-data:/data/db

volumes:
  fitjourney-mongo-data:
